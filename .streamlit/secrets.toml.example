# Kopieer dit bestand naar secrets.toml en vul je API key in
# BELANGRIJK: secrets.toml nooit committen naar GitHub! (staat in .gitignore)
#
# Op Streamlit Community Cloud stel je dit in via:
# App dashboard → Settings → Secrets

ANTHROPIC_API_KEY = "sk-ant-..."
